{
  "name": "thenines",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch": "node scripts/fetch-status.js",
    "seed": "node scripts/refresh-seed.js",
    "test": "node --test scripts/*.test.js && node scripts/smoke-test.js --offline",
    "test:live": "node --test scripts/*.test.js && node scripts/smoke-test.js",
    "typecheck": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2",
    "vite": "^6.4.3"
  }
}
